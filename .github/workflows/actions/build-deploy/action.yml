
name: "Build et déploiement"
description: "Compile et déploie les functions Firebase"

inputs:
  project-id:
    description: "ID du projet Firebase"
    required: true
  firebase-token:
    description: "Token Firebase CLI"
    required: true
  working-directory:
    description: "Répertoire de travail"
    required: true
  deploy-target:
    description: "Cible de déploiement (functions ou functionsV2)"
    required: false
    default: "functions"

runs:
  using: "composite"
  steps:
    - name: Mise à jour finale de Firebase CLI
      shell: bash
      run: |
        echo "🔧 Vérification finale de Firebase CLI avant déploiement..."
        CLI_VERSION=$(firebase --version | cut -d' ' -f1)
        
        if [[ "$CLI_VERSION" != "13.0.2" ]]; then
          echo "⚠️ Version incorrecte détectée: $CLI_VERSION. Installation forcée de Firebase CLI 13.0.2..."
          npm uninstall -g firebase-tools || true
          npm cache clean --force
          npm install -g firebase-tools@13.0.2 --force
          
          echo "Version après mise à jour forcée:"
          firebase --version
        fi
        
        echo "✅ Firebase CLI v13.0.2 correctement configurée"

    - name: Build et préparation des fonctions
      shell: bash
      working-directory: ${{ inputs.working-directory }}
      run: |
        echo "🔨 Compilation et préparation des fonctions..."
        
        # Nettoyage des répertoires
        rm -rf lib node_modules/firebase-admin/node_modules node_modules/firebase-functions/node_modules
        
        # Compilation TypeScript
        npm run build
        
        # Préparation du dossier lib pour le déploiement
        cp package.json lib/
        cp functions.yaml lib/
        
        # Création du .firebaserc
        echo "{\"projects\": {\"default\": \"${{ inputs.project-id }}\"}}" > lib/.firebaserc
        
        # Installer les dépendances dans le dossier lib
        cd lib
        FIREBASE_ADMIN_VERSION="13.0.2"
        FIREBASE_FUNCTIONS_VERSION="6.3.1"
        
        echo "📦 Installation des dépendances dans lib..."
        npm install --production --no-package-lock
        
        # Assurez-vous que firebase-admin et firebase-functions sont correctement installés
        npm install firebase-admin@${FIREBASE_ADMIN_VERSION} firebase-functions@${FIREBASE_FUNCTIONS_VERSION} --save --no-package-lock
        
        echo "📂 Vérification du contenu de lib/node_modules:"
        ls -la node_modules/firebase-admin || echo "❌ firebase-admin est manquant!"
        ls -la node_modules/firebase-functions || echo "❌ firebase-functions est manquant!"
        
        if [ ! -d "node_modules/firebase-admin" ]; then
          echo "❌ ERREUR CRITIQUE: firebase-admin n'est pas installé correctement. Installation forcée..."
          npm install firebase-admin@${FIREBASE_ADMIN_VERSION} --save --no-package-lock --force
        fi
        
        if [ ! -d "node_modules/firebase-functions" ]; then
          echo "❌ ERREUR CRITIQUE: firebase-functions n'est pas installé correctement. Installation forcée..."
          npm install firebase-functions@${FIREBASE_FUNCTIONS_VERSION} --save --no-package-lock --force
        fi
        
        # Vérifications finales
        echo "📝 Package.json dans lib:"
        cat package.json
        
        echo "📁 Structure des dossiers dans lib/node_modules:"
        ls -la node_modules | grep firebase

    - name: Déploiement avec CLI Firebase 13.0.2
      shell: bash
      working-directory: ${{ inputs.working-directory }}/lib
      run: |
        echo "🚀 Déploiement des fonctions sur ${{ inputs.project-id }}..."
        
        # Dernière vérification de CLI
        CLI_VERSION=$(firebase --version | cut -d' ' -f1)
        echo "Version Firebase CLI pour déploiement: $CLI_VERSION"
        
        # Si version incorrecte, mise à jour d'urgence
        if [[ "$CLI_VERSION" != "13.0.2" ]]; then
          echo "⚠️ Version Firebase CLI incorrecte, mise à jour d'urgence..."
          npm uninstall -g firebase-tools || true
          npm cache clean --force
          npm install -g firebase-tools@13.0.2 --force
          firebase --version
        fi
        
        # Déterminer la cible de déploiement
        if [ "${{ inputs.deploy-target }}" = "functionsV2" ]; then
          DEPLOY_TARGET="functions:v2"
          echo "Déploiement des functions v2"
        else
          DEPLOY_TARGET="functions"
          echo "Déploiement des functions standard"
        fi
        
        # Vérification critique finale avant déploiement
        if [ ! -d "node_modules/firebase-admin" ] || [ ! -d "node_modules/firebase-functions" ]; then
          echo "❌ ERREUR CRITIQUE: Modules Firebase manquants avant déploiement!"
          echo "Installation de secours des modules essentiels..."
          npm install firebase-admin@13.0.2 firebase-functions@6.3.1 --save --no-package-lock --force
        fi
        
        # Déploiement avec debug complet
        echo "🔍 Exécution du déploiement Firebase avec debug activé..."
        GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-service-account.json firebase deploy --only ${DEPLOY_TARGET} \
          --project ${{ inputs.project-id }} \
          --token "${{ inputs.firebase-token }}" \
          --non-interactive \
          --debug
