
name: "Build et déploiement"
description: "Compile et déploie les functions Firebase"

inputs:
  project-id:
    description: "ID du projet Firebase"
    required: true
  firebase-token:
    description: "Token Firebase CLI"
    required: true
  working-directory:
    description: "Répertoire de travail"
    required: true
  deploy-target:
    description: "Cible de déploiement (functions ou functionsV2)"
    required: false
    default: "functions"

runs:
  using: "composite"
  steps:
    - name: Mise à jour finale de Firebase CLI
      shell: bash
      run: |
        echo "🔧 Vérification finale de Firebase CLI avant déploiement..."
        CLI_VERSION=$(firebase --version | cut -d' ' -f1)
        
        if [[ "$CLI_VERSION" != "13.0.2" ]]; then
          echo "⚠️ Version incorrecte détectée: $CLI_VERSION. Installation forcée de Firebase CLI 13.0.2..."
          npm uninstall -g firebase-tools || true
          npm cache clean --force
          npm install -g firebase-tools@13.0.2 --force
          
          echo "Version après mise à jour forcée:"
          firebase --version
          
          # Vérification critique finale
          CLI_VERSION=$(firebase --version | cut -d' ' -f1)
          if [[ "$CLI_VERSION" != "13.0.2" ]]; then
            echo "❌ ERREUR CRITIQUE: Impossible d'installer la version correcte. Abandon."
            exit 1
          fi
        fi
        
        echo "✅ Firebase CLI v13.0.2 correctement configurée"

    - name: Build Functions
      shell: bash
      working-directory: ${{ inputs.working-directory }}
      run: |
        echo "🔨 Compilation des fonctions..."
        
        # Nettoyage du dossier lib
        rm -rf lib
        mkdir -p lib
        
        # Compilation directe
        npm run build
        
        if [ $? -ne 0 ]; then
          echo "❌ ERREUR de compilation"
          exit 1
        fi
        
        # Création d'un package.json complet dans lib avec toutes les dépendances
        echo "📦 Création du package.json pour lib avec versions compatibles..."
        echo '{
          "main": "index.js",
          "dependencies": {
            "firebase-admin": "13.0.2",
            "firebase-functions": "6.3.1",
            "@google-cloud/storage": "7.15.0",
            "openai": "4.28.0",
            "cors": "2.8.5",
            "dotenv": "16.3.1",
            "node-fetch": "2.6.12",
            "form-data": "4.0.2"
          }
        }' > lib/package.json
        
        echo "✅ Build terminé avec succès"
        echo "📄 package.json créé dans lib:"
        cat lib/package.json
        
        # Installation des dépendances directement dans le dossier lib
        echo "📦 Installation des dépendances dans le dossier lib..."
        cd lib
        npm install --production --no-optional --no-package-lock
        
        # Vérification de l'installation des modules critiques
        if [ ! -d "node_modules/firebase-admin" ] || [ ! -d "node_modules/firebase-functions" ]; then
          echo "⚠️ Modules Firebase manquants, installation manuelle..."
          npm install firebase-admin@13.0.2 firebase-functions@6.3.1 --save --no-package-lock
        fi
        
        cd ..
        
        # Copie du fichier functions.yaml dans lib
        cp functions.yaml lib/
        
        # Vérification des fichiers critiques
        if [ ! -f "lib/index.js" ]; then
          echo "❌ ERREUR: lib/index.js manquant!"
          exit 1
        fi
        
        echo "📂 Contenu final du dossier lib:"
        ls -la lib
        echo "📂 Vérification des modules Firebase:"
        ls -la lib/node_modules/firebase-admin || echo "Module firebase-admin manquant!"
        ls -la lib/node_modules/firebase-functions || echo "Module firebase-functions manquant!"

    - name: Configuration finale de Firebase
      shell: bash
      working-directory: ${{ inputs.working-directory }}
      run: |
        echo "🔧 Configuration de Firebase pour le projet ${{ inputs.project-id }}..."
        echo "{\"projects\": {\"default\": \"${{ inputs.project-id }}\"}}" > .firebaserc
        
        # Vérification que le fichier functions.yaml existe
        if [ ! -f "functions.yaml" ]; then
          echo "❌ functions.yaml manquant, création..."
          echo "# Configuration Firebase Functions" > functions.yaml
          echo "specVersion: v1" >> functions.yaml
          echo "functions:" >> functions.yaml
          echo "  - source: ." >> functions.yaml
          if [ "${{ inputs.deploy-target }}" = "functionsV2" ]; then
            echo "    codebase: v2" >> functions.yaml
          else
            echo "    codebase: default" >> functions.yaml
          fi
          echo "    runtime: nodejs20" >> functions.yaml
          echo "    memory: 1GB" >> functions.yaml
          echo "    timeout: 540s" >> functions.yaml
        fi
        
        echo "📄 Configuration functions.yaml finale:"
        cat functions.yaml
    
    - name: Déploiement avec CLI Firebase 13.0.2
      shell: bash
      working-directory: ${{ inputs.working-directory }}
      run: |
        echo "🚀 Déploiement des fonctions sur ${{ inputs.project-id }}..."
        
        # Dernière vérification de CLI
        CLI_VERSION=$(firebase --version | cut -d' ' -f1)
        echo "Version Firebase CLI pour déploiement: $CLI_VERSION"
        
        # Si version incorrecte, mise à jour d'urgence
        if [[ "$CLI_VERSION" != "13.0.2" ]]; then
          echo "⚠️ Version Firebase CLI incorrecte, mise à jour d'urgence..."
          npm uninstall -g firebase-tools || true
          npm cache clean --force
          npm install -g firebase-tools@13.0.2 --force
          firebase --version
        fi
        
        # Déploiement à partir du dossier lib
        cd lib
        
        # Création d'un .firebaserc minimal dans lib
        echo "{\"projects\": {\"default\": \"${{ inputs.project-id }}\"}}" > .firebaserc
        
        # Déterminer la cible de déploiement
        if [ "${{ inputs.deploy-target }}" = "functionsV2" ]; then
          DEPLOY_TARGET="functions:v2"
          echo "Déploiement des functions v2"
        else
          DEPLOY_TARGET="functions"
          echo "Déploiement des functions standard"
        fi
        
        # Déploiement avec debug complet
        echo "🔍 Exécution du déploiement Firebase avec debug activé..."
        firebase deploy --only ${DEPLOY_TARGET} \
          --project ${{ inputs.project-id }} \
          --token "${{ inputs.firebase-token }}" \
          --non-interactive \
          --debug
