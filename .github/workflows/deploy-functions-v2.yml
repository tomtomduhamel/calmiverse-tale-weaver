
name: Déploiement des Functions v2 Simplifiées
on:
  workflow_dispatch:
    inputs:
      environment:
        description: "Environnement de déploiement"
        required: true
        default: "production"
        type: choice
        options:
          - production
          - staging

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: ${{ github.event.inputs.environment }}
    
    steps:
      - name: Checkout du code
        uses: actions/checkout@v3
      
      - name: Configuration de Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'
      
      - name: Installation directe (ultra-simplifiée)
        working-directory: functions-v2
        run: |
          echo "📢 Début de l'installation ultra-simplifiée"
          
          echo "🧹 Nettoyage des dossiers..."
          rm -rf node_modules lib package-lock.json

          echo "📁 Vérification de la structure de répertoires..."
          mkdir -p functions-v2
          
          echo "📦 Installation des dépendances minimales (sans build)..."
          npm install --omit=dev
          
          echo "✅ Dépendances installées:"
          npm list --depth=0
          
          echo "📄 Vérification des fichiers importants:" 
          ls -la
          cat index.js
      
      - name: Configuration du compte de service Firebase
        uses: ./.github/workflows/actions/setup-firebase-service
        with:
          firebase-service-account: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
      
      - name: Installation de Firebase CLI
        run: npm install -g firebase-tools@13.0.2
      
      - name: Déploiement direct des Functions v2
        working-directory: functions-v2
        run: |
          echo "🚀 Début du déploiement des fonctions v2..."
          
          echo "📄 Vérification de l'index.js avant déploiement:"
          cat index.js
          
          echo "📄 Vérification de package.json avant déploiement:"
          cat package.json
          
          echo "📄 Vérification de functions.yaml avant déploiement:"
          cat functions.yaml
          
          echo "🔑 Utilisation des variables d'environnement:"
          echo "GOOGLE_APPLICATION_CREDENTIALS: $GOOGLE_APPLICATION_CREDENTIALS"
          echo "GOOGLE_CLOUD_PROJECT: ${{ secrets.GOOGLE_CLOUD_PROJECT || 'calmi-99482' }}"
          
          firebase deploy --only functions:v2 \
            --project ${{ secrets.GOOGLE_CLOUD_PROJECT || 'calmi-99482' }} \
            --token "${{ secrets.FIREBASE_TOKEN }}" \
            --debug
