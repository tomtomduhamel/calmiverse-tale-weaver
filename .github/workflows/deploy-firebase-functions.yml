
name: Déploiement Firebase Functions
on:
  workflow_dispatch:
    inputs:
      environment:
        description: "Environnement de déploiement"
        required: true
        default: "production"
        type: choice
        options:
          - production
          - staging

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: ${{ github.event.inputs.environment }}
    
    steps:
      - name: Checkout du code
        uses: actions/checkout@v3
      
      - name: Configuration de Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "20"
      
      - name: Installation explicite des dépendances critiques pour Functions V2
        working-directory: functions-v2
        run: |
          rm -rf node_modules lib
          rm -f package-lock.json
          echo "📦 Installation explicite des dépendances critiques..."
          npm install firebase-admin firebase-functions --no-package-lock
          npm list --depth=0
          echo "📦 Vérification de l'installation:"
          ls -la node_modules/firebase-admin
          ls -la node_modules/firebase-functions
      
      - name: Installation complète des dépendances Functions V2
        working-directory: functions-v2
        run: |
          npm install --no-package-lock --verbose --production
          echo "✅ Installation complète terminée"
          npm list --depth=0
          
          # Vérifier l'installation de firebase-admin
          if [ -d "node_modules/firebase-admin" ]; then
            echo "✅ firebase-admin est correctement installé"
            ls -la node_modules/firebase-admin
          else
            echo "❌ firebase-admin n'est PAS installé correctement!"
            npm install firebase-admin --save --no-package-lock
          fi
      
      - name: Configuration des variables d'environnement
        working-directory: functions-v2
        run: |
          echo "NODE_ENV=production" > .env
          echo "OPENAI_API_KEY=${{ secrets.OPENAI_API_KEY }}" >> .env
          echo "GOOGLE_CLOUD_PROJECT=${{ secrets.GOOGLE_CLOUD_PROJECT || 'calmi-99482' }}" >> .env
          
          echo "Variables d'environnement configurées avec succès"
          echo "GOOGLE_CLOUD_PROJECT: ${{ secrets.GOOGLE_CLOUD_PROJECT || 'calmi-99482' }}"
          echo "Présence de OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY != '' }}"
      
      - name: Configuration du compte de service Firebase
        env:
          FIREBASE_SERVICE_ACCOUNT_JSON: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
        run: |
          echo "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-service-account.json
          chmod 600 /tmp/firebase-service-account.json
          echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-service-account.json" >> $GITHUB_ENV
      
      - name: Installation de Firebase CLI
        run: npm install -g firebase-tools@latest
      
      - name: Build et préparation des Functions V2
        working-directory: functions-v2
        run: |
          npm run build
          echo "Build des fonctions v2 terminé"
          
          # Vérifier que l'index.js est correctement généré
          if [ ! -f "lib/index.js" ]; then
            echo "❌ lib/index.js n'existe pas après la compilation"
            exit 1
          fi
          
          # Copier les dépendances avec le script dédié
          npm run copy-deps
          
          # Vérifier que les dépendances sont bien copiées
          echo "Vérification des dépendances copiées:"
          ls -la lib/node_modules/firebase-admin
          ls -la lib/node_modules/firebase-functions
      
      - name: Déploiement des Functions V2
        working-directory: functions-v2
        run: |
          echo "Déploiement des fonctions v2 sur ${{ secrets.GOOGLE_CLOUD_PROJECT || 'calmi-99482' }}..."
          
          # Vérifier une dernière fois la présence des modules critiques
          if [ ! -d "lib/node_modules/firebase-admin" ]; then
            echo "⚠️ firebase-admin n'est pas dans lib/node_modules, copie d'urgence..."
            mkdir -p lib/node_modules
            cp -r node_modules/firebase-admin lib/node_modules/
          fi
          
          if [ ! -d "lib/node_modules/firebase-functions" ]; then
            echo "⚠️ firebase-functions n'est pas dans lib/node_modules, copie d'urgence..."
            mkdir -p lib/node_modules
            cp -r node_modules/firebase-functions lib/node_modules/
          fi
          
          # Ajouter NODE_PATH pour aider à trouver les modules lors du déploiement
          export NODE_PATH="$PWD/lib/node_modules:$NODE_PATH"
          echo "NODE_PATH=$NODE_PATH"
          
          firebase deploy --only functions:v2 \
            --project ${{ secrets.GOOGLE_CLOUD_PROJECT || 'calmi-99482' }} \
            --token "${{ secrets.FIREBASE_TOKEN }}" \
            --non-interactive \
            --debug
        env:
          GOOGLE_APPLICATION_CREDENTIALS: /tmp/firebase-service-account.json
