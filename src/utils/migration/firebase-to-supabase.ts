
/**
 * @deprecated Ce fichier est maintenu uniquement pour la compatibilité.
 */

export const migrateFirebaseUser = async () => {
  console.warn("La fonction migrateFirebaseUser est obsolète. La migration a été terminée.");
  return { success: true, message: "Migration terminée" };
};

export const migrateUserData = async () => {
  console.warn("La fonction migrateUserData est obsolète. La migration a été terminée.");
  return { success: true, message: "Migration terminée" };
};
